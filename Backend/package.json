{
  "name": "campus-connection-backend",
  "version": "1.0.0",
  "description": "Backend API para Campus Connection App",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "nodemon --exec \"node --env-file=.env\" src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["express", "api", "campus"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.0.0",
    "nodemailer": "^8.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
